{
  "name": "snipertrader-ai",
  "version": "1.0.0",
  "private": true,
  "description": "SniperTrader.ai — static site + Kronos Foundation Model serverless API.",
  "engines": { "node": ">=18" },
  "scripts": {
    "dev:dashboard": "npm run dev --prefix frontend",
    "build:dashboard": "npm run build --prefix frontend"
  }
}
