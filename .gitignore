# Secrets — never commit real keys
api/.env

# Hermes agent working directory
.hermes/

# Node
node_modules/
npm-debug.log

# macOS
.DS_Store

# Screenshot/build artifacts (regenerated by the .shots/ QA harness)
.shots/

# Recon backtest outputs (multi-MB, rebuilt by the teardown/ pipeline)
teardown/*.json
teardown/terminal.html
teardown/shot_*.png
teardown/shot_*.jpg
